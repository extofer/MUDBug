@model IEnumerable<MUDBug.Models.MUDBugRestart>

@{
    ViewBag.Title = "Index";
}

<h2>Restart</h2>


<table>
    <tr>
        <th>
            EventDate
        </th>
        <th>
            Event
        </th>

    </tr>

@foreach (var item in Model) {
    <tr>
        
        <td>
            @Html.DisplayFor(modelItem => item.EventDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EVENT)
        </td>
        
    </tr>
}

</table>
